%h1= @asset.name

= simple_form_for @asset, multipart: true, url: bold_asset_path(@asset) do |f|
  = render 'form', f: f
  .buttons{:style => "margin-top: 0;"}
    %span.status
    = button '.save', 'primary btn-sm'

