- content_for(:body_id){'homepage'}
= render 'themes/test/navigation'

- for post in post_list
  %article
    %h3= post.link_to
    - if post.teaser?
      .teaser= post.teaser

    %aside
      Posted by #{post.author_link} on #{post.post_date}

= post_list.pagination
