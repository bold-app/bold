.row
  .col-sm-9.col-sm-offset-1.col-md-8

    %h2= t'.heading'

    %p= raw t '.current_theme', name: h(current_site.theme.name)
    %p= t '.info'
    %br/

    .list-group.no-border.multiple

      - @themes.each do |theme|
        = render 'theme', theme: theme
